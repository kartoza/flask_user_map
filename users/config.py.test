# coding=utf-8
"""Module for saving all configuration."""

MAIL_CONFIG = dict(
    TESTING=True,
    MAIL_SERVER='127.0.0.1',
    MAIL_PORT=25,
    MAIL_USE_TLS=True,
    MAIL_USE_SSL=False,
    MAIL_USERNAME='',
    MAIL_PASSWORD='',
)
# MAIL ADMINISTRATOR
MAIL_ADMIN = ('User Map Administrator', 'foo@bar.com')
